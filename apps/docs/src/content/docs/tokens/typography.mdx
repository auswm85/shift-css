---
title: Typography Tokens
description: Font families, sizes, weights, and line heights
---

## Font Families

```css
:root {
  --font-sans: system-ui, -apple-system, BlinkMacSystemFont,
               'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
  --font-serif: Georgia, Cambria, 'Times New Roman', Times, serif;
  --font-mono: ui-monospace, SFMono-Regular, 'SF Mono', Menlo,
               Consolas, 'Liberation Mono', monospace;
}
```

### Customizing Fonts

```css
:root {
  --font-sans: 'Inter', system-ui, sans-serif;
  --font-mono: 'JetBrains Mono', monospace;
}
```

## Font Sizes

Fluid typography that scales with viewport:

| Token | Min Size | Max Size | Class |
|-------|----------|----------|-------|
| `--text-xs` | 0.75rem | 0.75rem | `.text-xs` |
| `--text-sm` | 0.875rem | 0.875rem | `.text-sm` |
| `--text-base` | 1rem | 1rem | `.text-base` |
| `--text-lg` | 1.125rem | 1.125rem | `.text-lg` |
| `--text-xl` | 1.25rem | 1.25rem | `.text-xl` |
| `--text-2xl` | 1.5rem | 1.5rem | `.text-2xl` |
| `--text-3xl` | 1.875rem | 1.875rem | `.text-3xl` |
| `--text-4xl` | 2.25rem | 2.25rem | `.text-4xl` |
| `--text-5xl` | 3rem | 3rem | `.text-5xl` |
| `--text-6xl` | 3.75rem | 3.75rem | `.text-6xl` |
| `--text-7xl` | 4.5rem | 4.5rem | `.text-7xl` |
| `--text-8xl` | 6rem | 6rem | `.text-8xl` |
| `--text-9xl` | 8rem | 8rem | `.text-9xl` |

### Using Font Size Tokens

```css
.headline {
  font-size: var(--text-4xl);
}

.caption {
  font-size: var(--text-sm);
}
```

## Font Weights

| Token | Value | Class |
|-------|-------|-------|
| `--font-thin` | 100 | `.font-thin` |
| `--font-extralight` | 200 | `.font-extralight` |
| `--font-light` | 300 | `.font-light` |
| `--font-normal` | 400 | `.font-normal` |
| `--font-medium` | 500 | `.font-medium` |
| `--font-semibold` | 600 | `.font-semibold` |
| `--font-bold` | 700 | `.font-bold` |
| `--font-extrabold` | 800 | `.font-extrabold` |
| `--font-black` | 900 | `.font-black` |

### Using Font Weight Tokens

```css
.title {
  font-weight: var(--font-bold);
}

.subtitle {
  font-weight: var(--font-medium);
}
```

## Line Heights

| Token | Value | Class |
|-------|-------|-------|
| `--leading-none` | 1 | `.leading-none` |
| `--leading-tight` | 1.25 | `.leading-tight` |
| `--leading-snug` | 1.375 | `.leading-snug` |
| `--leading-normal` | 1.5 | `.leading-normal` |
| `--leading-relaxed` | 1.625 | `.leading-relaxed` |
| `--leading-loose` | 2 | `.leading-loose` |

### Using Line Height Tokens

```css
.body-text {
  line-height: var(--leading-relaxed);
}

.display-text {
  line-height: var(--leading-tight);
}
```

## Letter Spacing

| Token | Value | Class |
|-------|-------|-------|
| `--tracking-tighter` | -0.05em | `.tracking-tighter` |
| `--tracking-tight` | -0.025em | `.tracking-tight` |
| `--tracking-normal` | 0em | `.tracking-normal` |
| `--tracking-wide` | 0.025em | `.tracking-wide` |
| `--tracking-wider` | 0.05em | `.tracking-wider` |
| `--tracking-widest` | 0.1em | `.tracking-widest` |

## Typography Presets

### Headings

```css
h1 {
  font-size: var(--text-4xl);
  font-weight: var(--font-bold);
  line-height: var(--leading-tight);
  letter-spacing: var(--tracking-tight);
}

h2 {
  font-size: var(--text-3xl);
  font-weight: var(--font-bold);
  line-height: var(--leading-tight);
}

h3 {
  font-size: var(--text-2xl);
  font-weight: var(--font-semibold);
  line-height: var(--leading-snug);
}
```

### Body Text

```css
body {
  font-family: var(--font-sans);
  font-size: var(--text-base);
  font-weight: var(--font-normal);
  line-height: var(--leading-normal);
}
```

### Code

```css
code, pre {
  font-family: var(--font-mono);
  font-size: var(--text-sm);
}
```

## Customizing Typography

### Base Size

Changing the base size affects all relative typography:

```css
:root {
  --text-base: 1.125rem;  /* Larger base (18px) */
}
```

### Custom Scale

```css
:root {
  --text-xl: 1.5rem;
  --text-2xl: 2rem;
  --text-3xl: 2.5rem;
}
```

## Responsive Typography

Typography utilities support responsive prefixes:

```html
<h1 class="text-2xl md:text-4xl lg:text-5xl">
  Responsive heading
</h1>
```

## Best Practices

### Hierarchy

Use consistent sizing for visual hierarchy:

```css
.page-title { font-size: var(--text-4xl); }
.section-title { font-size: var(--text-2xl); }
.card-title { font-size: var(--text-lg); }
.body-text { font-size: var(--text-base); }
.caption { font-size: var(--text-sm); }
```

### Readability

- Body text: 16-18px with 1.5-1.75 line height
- Long-form content: Limit line length to 60-75 characters
- Use appropriate contrast ratios (4.5:1 minimum)
