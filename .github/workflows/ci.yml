name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run linter
        run: bun run lint

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build packages
        run: bun run build

      - name: Check bundle size
        run: |
          MINIFIED_SIZE=$(stat -c%s packages/core/dist/shift.min.css 2>/dev/null || stat -f%z packages/core/dist/shift.min.css)
          MINIFIED_KB=$(echo "scale=2; $MINIFIED_SIZE / 1024" | bc)
          GZIP_SIZE=$(gzip -c packages/core/dist/shift.min.css | wc -c)
          GZIP_KB=$(echo "scale=2; $GZIP_SIZE / 1024" | bc)

          echo "📊 Bundle Size Report"
          echo "====================="
          echo "Minified: ${MINIFIED_KB} KB"
          echo "Gzipped:  ${GZIP_KB} KB"

          # Fail if gzipped size exceeds 15KB target
          MAX_SIZE=15360
          if [ "$GZIP_SIZE" -gt "$MAX_SIZE" ]; then
            echo "❌ Bundle exceeds 15KB gzipped target!"
            exit 1
          else
            echo "✅ Bundle size within target (<15KB gzipped)"
          fi

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: packages/core/dist/
          retention-days: 7
